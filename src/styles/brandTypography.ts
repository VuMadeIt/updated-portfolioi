import clsx from "clsx";

/** Matches FooterBrand — lucas vu wordmark */
export const brandNameClass = clsx(
  "font-['Lucas',sans-serif] font-medium leading-normal text-[#3f3f46] text-3xl",
);

/** Matches footer secondary copy (time, contact labels, etc.) */
export const brandSubtextClass = clsx(
  "font-['Lucas',sans-serif] font-normal leading-normal text-zinc-400 text-base",
);
